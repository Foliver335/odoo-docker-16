<odoo>
  <data>
    <record id="mail_tmpl_fiscal_authorized" model="mail.template">
      <field name="name">Fiscal Authorized</field>
      <field name="model_id" ref="nvt_fiscal.model_finance_fiscal_document"/>
      <field name="subject">Nota Fiscal ${object.name} autorizada</field>
      <field name="email_from">${(object.company_id.email_formatted or '')}</field>
      <field name="email_to">${(object.partner_id.email or '')}</field>
      <field name="body_html"><![CDATA[
        <p>Olá,</p>
        <p>Sua Nota Fiscal <strong>${object.name}</strong> foi <strong>autorizada</strong>.</p>
        <p>Total: <strong>${format_amount(object.amount_total, object.currency_id)}</strong></p>
      ]]></field>
      <field name="auto_delete" eval="True"/>
    </record>
  </data>
</odoo>
